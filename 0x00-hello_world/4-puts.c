#include <stdio.h>

/**
 * main - A function to output a string
 * Return: 0 (Success)
 */
int main(void)
{
	char str1[] = "\"Programming is like building a multilingual puzzle";

	puts(str1);

	return (0);
}
